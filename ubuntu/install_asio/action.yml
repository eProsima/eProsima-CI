name: 'install_asio'

runs:
  using: composite
  steps:

    - name: Install in ubuntu
      uses: eProsima/eProsima-CI/ubuntu/install_apt_packages@build_w_tsan
      with:
        packages: libasio-dev
