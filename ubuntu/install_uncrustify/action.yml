name: install_uncrustify

runs:
  using: composite
  steps:

    - name: Install uncrustify
      uses: eProsima/eProsima-CI/ubuntu/install_apt_packages@feature/artifact-build
      with:
        packages: uncrustify
