name: install_openssl
description: Install Open SSL

runs:
  using: composite
  steps:

    - name: Run in ubuntu
      uses: eProsima/eProsima-CI/ubuntu/install_openssl@v0.42.0
      if: runner.os == 'Linux'

    - name: Run in windows
      uses: eProsima/eProsima-CI/windows/install_openssl@v0.42.0
      if: runner.os == 'Windows'
