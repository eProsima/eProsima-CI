name: install_openssl
description: Install Open SSL

runs:
  using: composite
  steps:

    - name: Run in ubuntu
      uses: eProsima/eProsima-CI/ubuntu/install_openssl@hotfix/python
      if: runner.os == 'Linux'

    - name: Run in windows
      uses: eProsima/eProsima-CI/windows/install_openssl@hotfix/python
      if: runner.os == 'Windows'
